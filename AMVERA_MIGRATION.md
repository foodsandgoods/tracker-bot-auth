# 🚀 Миграция бота на Amvera Cloud

Пошаговая инструкция для переноса с Render.com на Amvera.

---

## 📋 Содержание

1. [Подготовка](#1-подготовка)
2. [Регистрация на Amvera](#2-регистрация-на-amvera)
3. [Создание PostgreSQL](#3-создание-postgresql)
4. [Перенос данных из БД](#4-перенос-данных-из-бд)
5. [Создание приложения](#5-создание-приложения)
6. [Настройка переменных окружения](#6-настройка-переменных-окружения)
7. [Деплой кода](#7-деплой-кода)
8. [Проверка работы](#8-проверка-работы)
9. [Отключение Render](#9-отключение-render)

---

## 1. Подготовка

### 1.1 Сохрани текущие переменные окружения с Render

Зайди на https://dashboard.render.com → твой сервис → **Environment**

Скопируй все переменные (они понадобятся):
```
BOT_TOKEN=...
BASE_URL=...
DATABASE_URL=...
YANDEX_CLIENT_ID=...
YANDEX_CLIENT_SECRET=...
YANDEX_ORG_ID=...
GPTUNNEL_API_KEY=...
```

### 1.2 Проверь что amvera.yml добавлен в репозиторий

Файл уже создан в проекте. Закоммить его:
```bash
git add amvera.yml
git commit -m "add amvera config"
git push
```

---

## 2. Регистрация на Amvera

### 2.1 Создай аккаунт

1. Открой https://amvera.ru
2. Нажми **"Войти"** → **"Регистрация"**
3. Введи email и пароль
4. Подтверди email по ссылке из письма

### 2.2 Пополни баланс

1. В личном кабинете → **"Баланс"**
2. Пополни на ~500-1000₽ для начала (хватит на месяц)
3. Можно оплатить картой РФ

---

## 3. Создание PostgreSQL

### 3.1 Создай базу данных

1. В личном кабинете → **"Создать проект"**
2. Выбери **"База данных"** → **"PostgreSQL"**
3. Настройки:
   - **Имя проекта:** `tracker-bot-db`
   - **Тариф:** Начальный (512 МБ) — ~200₽/мес (достаточно для маленькой БД)
   - **Версия:** 15 или 16
4. Нажми **"Создать"**

### 3.2 Дождись запуска (1-2 минуты)

После запуска увидишь:
- **Хост:** что-то вроде `tracker-bot-db-...amvera.tech`
- **Порт:** `5432`
- **Пользователь:** обычно `postgres`
- **Пароль:** сгенерированный (скопируй!)
- **База:** `postgres` или создашь свою

### 3.3 Запиши строку подключения

Формат DATABASE_URL:
```
postgresql://postgres:ПАРОЛЬ@tracker-bot-db-XXX.db-msk0.amvera.tech:5432/postgres
```

---

## 4. Перенос данных из БД

### 4.1 Экспорт данных с Render

**Вариант A: Через psql (если установлен)**

1. Открой терминал
2. Экспортируй данные:
```bash
pg_dump "postgresql://USER:PASS@HOST:PORT/DATABASE" --format=plain --no-owner --no-acl > backup.sql
```

Замени `USER:PASS@HOST:PORT/DATABASE` на данные из Render DATABASE_URL.

**Вариант B: Через веб-интерфейс Render**

1. Render Dashboard → твоя база данных
2. **Info** → скопируй **External Database URL**
3. Используй любой PostgreSQL клиент (DBeaver, pgAdmin) для экспорта

### 4.2 Импорт данных в Amvera

```bash
psql "postgresql://postgres:ПАРОЛЬ@tracker-bot-db-XXX.db-msk0.amvera.tech:5432/postgres" < backup.sql
```

**Альтернатива:** Если данных мало — таблицы создадутся автоматически при первом запуске бота (код делает `ensure_schema`).

---

## 5. Создание приложения

### 5.1 Создай новый проект для бота

1. В личном кабинете Amvera → **"Создать проект"**
2. Выбери **"Приложение"**
3. Настройки:
   - **Имя проекта:** `tracker-bot`
   - **Тариф:** Стандартный (2 ГБ RAM, 2 CPU) — ~1450₽/мес
   - **Тип:** Приложение (не сайт)
4. Нажми **"Создать"**

### 5.2 Подключи Git репозиторий

**Вариант A: Через GitHub**

1. В проекте → **"Настройки"** → **"Репозиторий"**
2. Нажми **"Подключить GitHub"**
3. Авторизуй Amvera в GitHub
4. Выбери репозиторий `tracker-bot-auth`
5. Ветка: `main`

**Вариант B: Через встроенный Git Amvera**

1. В проекте → **"Репозиторий"** → скопируй Git URL
   Будет что-то вроде: `https://git.amvera.ru/username/tracker-bot.git`
2. Добавь remote:
```bash
git remote add amvera https://git.amvera.ru/username/tracker-bot.git
```
3. Пуш:
```bash
git push amvera main
```

---

## 6. Настройка переменных окружения

### 6.1 Добавь секреты

1. В проекте → **"Переменные"** или **"Секреты"**
2. Добавь каждую переменную:

| Переменная | Значение |
|------------|----------|
| `BOT_TOKEN` | Токен твоего бота из @BotFather |
| `BASE_URL` | `https://tracker-bot-XXX.amvera.io` (узнаешь после деплоя) |
| `DATABASE_URL` | Строка подключения к Amvera PostgreSQL |
| `YANDEX_CLIENT_ID` | Из Yandex OAuth |
| `YANDEX_CLIENT_SECRET` | Из Yandex OAuth |
| `YANDEX_ORG_ID` | ID организации в Tracker |
| `GPTUNNEL_API_KEY` | Ключ GPTunnel (если используется) |

### 6.2 Важно про BASE_URL

После первого деплоя Amvera выдаст URL вида:
```
https://tracker-bot-username.amvera.io
```

Обнови `BASE_URL` на этот адрес!

---

## 7. Деплой кода

### 7.1 Запусти сборку

1. После подключения репозитория → нажми **"Собрать"** или **"Деплой"**
2. Amvera:
   - Прочитает `amvera.yml`
   - Установит Python 3.11
   - Установит зависимости из `requirements.txt`
   - Запустит `python bot_web.py`

### 7.2 Следи за логами

1. В проекте → **"Логи"**
2. Должно появиться:
```
Starting services...
HTTP client initialized
Database pool initialized (1-5 connections)
Database schema ensured
Service layer initialized
```

### 7.3 Если ошибки

- **"Database connection failed"** — проверь DATABASE_URL
- **"BOT_TOKEN not set"** — проверь переменные окружения
- **"Port already in use"** — порт 10000 указан в `amvera.yml`

---

## 8. Проверка работы

### 8.1 Проверь веб-сервис

Открой в браузере:
```
https://tracker-bot-username.amvera.io/ping
```

Должен ответить: `pong`

### 8.2 Проверь бота

1. Открой Telegram
2. Найди своего бота
3. Отправь `/start`
4. Проверь `/me`, `/settings`, `/cl_my`

### 8.3 Обнови OAuth callback

⚠️ **Важно!** Обнови redirect URI в Yandex OAuth:

1. Зайди на https://oauth.yandex.ru/
2. Найди своё приложение
3. Обнови **Callback URL** на:
```
https://tracker-bot-username.amvera.io/oauth/callback
```

---

## 9. Отключение Render

После успешной проверки на Amvera:

### 9.1 Останови сервисы на Render

1. Render Dashboard → твой Web Service → **Settings** → **Delete Service**
2. Render Dashboard → твоя Database → **Settings** → **Delete Database**

### 9.2 Готово! 🎉

Бот теперь работает на Amvera в России.

---

## 💰 Итоговая стоимость

| Компонент | Тариф | Цена/мес |
|-----------|-------|----------|
| Приложение (бот) | Стандартный (2GB RAM) | ~1450₽ |
| PostgreSQL | Начальный (512MB) | ~200₽ |
| **Итого** | | **~1650₽/мес** |

Можно выбрать тариф побольше если нужно:
- Расширенный (4GB RAM): ~2900₽
- PostgreSQL Стандартный (1GB): ~400₽

---

## ❓ Частые вопросы

**Q: Бот не отвечает после деплоя**
A: Проверь логи, убедись что BOT_TOKEN правильный

**Q: Ошибка подключения к БД**
A: Проверь что DATABASE_URL в формате `postgresql://...` (не `postgres://`)

**Q: Как обновить код?**
A: `git push` в подключенный репозиторий — Amvera автоматически пересоберёт

**Q: Где посмотреть URL приложения?**
A: В проекте → "Обзор" или "Домены"

---

## 📞 Поддержка Amvera

- Документация: https://docs.amvera.ru
- Telegram: @amvera_support
- Email: support@amvera.ru
